# Staging / EAS "preview" build profile. Real values live in EAS secrets
# (see eas.json build profile `env` block) for CI builds; this file is for
# local `APP_ENV=staging npx expo start` testing against the staging API.

API_BASE_URL=https://staging-api.parkconnect.app
SCAN_BASE_URL=https://staging.parkconnect.app

USE_MOCK=false

SENTRY_DSN=
FCM_SENDER_ID=
EAS_PROJECT_ID=
